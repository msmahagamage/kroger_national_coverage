# Web map

`index.html` is a self-contained data page generated by `scripts/build_all.py`.
It embeds the location records, so it does not depend on absolute local paths.
The basemap tiles and Leaflet library load from public HTTPS services.

To publish with GitHub Pages, select the `main` branch and `/docs` folder in
the repository Pages settings.

